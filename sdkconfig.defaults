# Increase USB-CDC TX buffer to reduce frame loss at 500 Hz
# Default is 512 bytes (~6 frames at 80 bytes/frame).
# 4096 bytes ≈ 50 frames ≈ 100 ms of buffer at 500 Hz.
CONFIG_TINYUSB_CDC_TX_BUFSIZE=4096
CONFIG_TINYUSB_CDC_RX_BUFSIZE=4096
